# accounts - metadisk accounts daemon
#
description "Metadisk websocket daemon"

start on (started networking)
stop on runlevel [!2345]

respawn

chdir /home/<%= @user %>/metadisk-websockets
setuid <%= @user %>
setgid <%= @user %>

exec /home/<%= @user %>/metadisk-websockets/.env/bin/python index.py
