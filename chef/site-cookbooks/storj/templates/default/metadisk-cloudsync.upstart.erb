# cloudsync - metadisk synchronization daemon
#
description "Metadisk cloudsync daemon"

start on (started networing)
stop on runlevel [!2345]

respawn

chdir /home/<%= @user %>/web-core
setuid <%= @user %>
setgid <%= @user %>

exec /home/<%= @user %>/web-core/.env/bin/python cloudsync.py
